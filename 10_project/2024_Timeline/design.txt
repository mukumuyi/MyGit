1st Goal LITHO02 の最新スケジュール結果を見られるHPを提供する。（毛利くん踏み台を利用する。）
 1.site上のファイルを初期参照するように変更する。 -> app
  get method "/api/file/:filename" で所定のフォルダ"./TL_DATA"内の[:filename]を取得できるので、
  案(1) 初期処理でrequestを投げる仕様にする。（トランザクションがウザイ・・・）
  案(2) distフォルダからLocalのファイルを取得する。（CORの問題ないか？）
  -> 案(1)で実装。
 2.初期設定をRTSに合わせてカスタマイズする。
 3.CSVを定例的に出力する処理を作る。(サーバー側の処理構築)
 
2nd Goal とりあえず、動かしてみて、ゴールは決める。（翔太に見てみてもらう。）
 複数のALSを切り替えて選べるようにする。 
 ムダｎフォントは削除したいけど・・・
 画面の項目名も簡単に変更できるようにしておきたいけど・・・
 DBからの読込機能とか・・・(画面上、非表示設定にしている。 )

欠陥
001.InputHttp 再生ボタンのクリックが効かない。
 -> 問題はなし。ただ取り込み完了したかが分かりにくい。
  -> 取り込み完了時にAlertを出す。
002.取込完了後にカラム選択画面に行かせたい。


